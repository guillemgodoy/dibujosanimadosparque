#include <iostream>
#include <string>

using namespace std;

int main()
{
  string s;
  cin>>s;
  for (int i=0;i<int(s.size());i++) {
    if (s[i]!='o') cout<<s[i]<<s[i];
    else cout<<s[i]<<s[i]<<s[i]<<s[i];
  }
  cout<<endl;
}
